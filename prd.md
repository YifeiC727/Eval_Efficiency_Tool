# 评测数据自动汇总工具 PRD

## 一、背景与目标

视频生成评测流程中，多位评测人对 prompt 对应的两个模型生成结果进行对比打分。当前汇总分析需要手动完成，流程重复且易出错。

**目标**：构建自动化工具，仅需上传**原始数据汇总表** + **测评人信息表**，结合预配置的维度体系与标签体系，即可自动产出全部汇总表。

---

## 二、评测场景

系统支持三套评测标准，根据场景切换：

| 场景 | 说明 | 一级维度 | 是否有影响因素 | 是否产出二级维度汇总 |
|------|------|---------|--------------|-------------------|
| **T2V** | 文生视频 | 指令遵从与增益、动态与物理合理性、镜头语言与跨镜连贯、视觉美学、综合评价 | 是 | 是 |
| **T2AV** | 文生音视频 | T2V + 声音效果 | 是 | 是 |
| **WM** | World Model | 语义跟随、初始世界一致性、运动性、空间时序一致性、视觉美学、综合评价 | 取决于数据 | 取决于数据 |

配置来源：`视频生成通用评测维度汇总.xlsx`

**场景间关键差异**：

| 差异点 | T2V / T2AV | WM |
|--------|-----------|-----|
| 打分列后是否有影响因素列 | 有（每个维度后跟一列 JSON 数组） | 无 |
| 是否产出"二级维度汇总" | 是 | 否 |
| 标签体系 | 6套（主体类型、人物运动等） | 1套（task_type） |
| 测评人信息结构 | 可能有1组/2组配对 | 单列姓名列表 |
| 打分选项文本 | `G (Good)` / `B (Bad)` | `G（Good）` / `B（Bad）`（全角括号） |

---

## 三、系统配置

### 配置 1：评测维度体系

定义当前场景启用的一级维度及其二级维度（影响因素选项，仅 T2V/T2AV 有）。

**T2V/T2AV**：

| 一级维度 | 二级维度（影响因素选项） |
|---------|----------------------|
| 指令遵从与增益 | 主体遵从、动作/事件遵从、属性遵从、风格/氛围遵从、镜头指令遵从、意图增益度 |
| 动态与物理合理性 | 运动结构合理性、物理与常识合理性、时序稳定性、运动强度适配 |
| 镜头语言与跨镜连贯 | 跨镜头主体一致性、跨镜头时空连贯性、分镜合理性、运镜质量 |
| 视觉美学 | 真实感、整体协调性、主体美感、构图、光影质感、色彩表现、场景/环境美感 |
| 综合评价 | 指令遵从与增益、动态与物理合理性、镜头语言与跨镜连贯、视觉美学 |

T2AV 额外增加：声音效果（基础音频音质、音画同步与声源匹配、多镜头音频连续性、声音真实感与表现力）；指令遵从中增加 speech指令遵从、BGM/音效指令遵从。

**WM**：

| 一级维度 | 二级维度 |
|---------|---------|
| 语义跟随 | 核心动作达成度、运动控制遵从度、指令歧义处理、主体属性匹配 |
| 初始世界一致性 | 初始画面还原度、初始主体一致性、初始场景与空间一致性 |
| 运动性 | 动态程度、运动连续性与稳定性、运动物理合理性 |
| 空间时序一致性 | 空间一致性、主体一致性、长时稳定性、未观测区域推理 |
| 视觉美学 | 成像质量、美学质量、风格忠实与一致性、表观真实感 |
| 综合评价 | — |

> WM 的二级维度定义供参考。是否产出"二级维度汇总"取决于数据中是否有影响因素列（维度列后跟空表头列），不按场景硬性区分。

---

### 配置 2：标签体系

定义 prompt 的分类标签系统，用于"维度汇总"的分组统计。**不同评测场景使用不同标签体系**。

**T2V/T2AV 标签体系**：

| 标签体系 | 是否多选 | 标签值 |
|---------|---------|--------|
| 主体类型 | 是 | 人物、动物、物体、自然景观、人造空间、抽象形体、文字为主 |
| 人物运动 | 是 | 日常行为、社会互动、运动/体育、表演/艺术、极限动作、精细操作、情绪/表情 |
| 能力专项 | 否 | 动漫动画、多个体/群体画面、属性绑定、打斗场景、文字生成、材质质感、画面构图、概念重组、视觉特效、风格化 |
| 非人运动 | 否 | 静态/微动、刚体位移、动物运动、流体/软体、形态变化 |
| prompt类型 | 否 | 简单/开放、叙述性描述、专业影视语言、分镜脚本 |
| 镜头语言 | 是 | 运镜、多镜头、时间操控 |

**WM 标签体系**：

| 标签体系 | 是否多选 | 标签值 |
|---------|---------|--------|
| task_type | 否 | autonomous_driving、embodied_robot、indoor_home、multi_social、natural_outdoor、virtual_world |

---

## 四、数据输入

### 输入 1（核心）：原始数据汇总表

一张扁平表，每行 = 一个评测人对一条 prompt 的打分记录。

#### T2V/T2AV 表结构

| 列区域 | 列名 | 说明 |
|--------|------|------|
| Col 1 | 序号 | prompt 编号（1~N），每个评测人重复一轮 |
| Col 2 | prompt | prompt 文本 |
| Col 3+ | 标签列（多列） | 主体类型(主+副)、人物运动(主+副)、能力专项(主+副)、非人运动、prompt类型、镜头语言(主+副×2) |
| 接续 | 模型A / 模型B | 视频 URL |
| 接续 | [一级维度1] + 影响因素 | 打分 + JSON 数组，交替排列 |
| 接续 | [一级维度2] + 影响因素 | 打分 + JSON 数组 |
| ... | ... | ... |

#### WM 表结构

| 列区域 | 列名 | 说明 |
|--------|------|------|
| Col 1 | 序号 | prompt 编号（1~N） |
| Col 2 | task_type | 标签（单列，单选） |
| Col 3-5 | prompt_env / prompt_char / prompt_turns | prompt 相关文本 |
| Col 6 | source_image_url | 输入图像 URL |
| Col 7 | 模型A | 模型 A 视频 URL |
| Col 8 | 模型B | 模型 B 视频 URL |
| Col 9-14 | 语义跟随 / 初始世界一致性 / 运动性 / 空间时序一致性 / 视觉美学 / 综合评价 | 仅打分，无影响因素列 |

#### 通用特征

- 总行数 = 评测人数 × prompt 总数（WM示例：12 × 200 = 2400 行）
- 按评测人顺序排列，每人一段连续数据
- 评测人数和 prompt 总数由系统自动推断（序号列回到1时即为新一轮）
- 标签列每个评测人的内容一致（标签是 prompt 固有属性）

#### 原始数据汇总与个人数据汇总的关系

两个表是**同一份数据的不同排列方式**，可一一对应：

```
原始数据汇总（纵向排列）:          个人数据汇总（横向排列）:
行1-N:     评测人1的打分             每行=1条prompt
行N+1-2N:  评测人2的打分             横排所有评测人的打分列
行2N+1-3N: 评测人3的打分             [综合列] | [人1各维度+因素] | [人2各维度+因素] | ...
...
```

系统只需读取**原始数据汇总**即可完成所有计算，个人数据汇总作为输出产物自动生成。

#### 双人配对模式说明

当测评人信息表中有两组姓名（表头出现两个"姓名"列，且两侧人数相等）时：
- 每对评测人（如"周海清/张一鸣"）的数据**已合并为一段**存在原始数据汇总中
- 周海清做前半题、张一鸣做后半题，但拼接为完整的 N 行数据
- 从系统角度看等同于"一个评测人做全量题"
- 输出中使用"A/B"格式显示名称（如"周海清/张一鸣"）

---

### 输入 2（辅助）：测评人信息表

提供评测人姓名，用于在输出中标识每个评测人。

**可能的两种结构**：

**结构 A：单列**（WM 示例）

| 序号 | 姓名 | 性别 | 年龄 | 学历 | 院校 | 专业 |
|------|------|------|------|------|------|------|
| 1 | 张一鸣 | 男 | 22 | ... | ... | ... |
| 2 | 杨旸 | 女 | 23 | ... | ... | ... |

→ 输出中直接使用姓名

**结构 B：双列配对**（T2V 示例）

| 1组序号 | 1组姓名 | ... | 2组序号 | 2组姓名 | ... |
|--------|---------|-----|--------|---------|-----|
| 1 | 周海清 | ... | 1 | 张一鸣 | ... |
| 2 | 陈润虹 | ... | 2 | 杨旸 | ... |

→ 输出中以"1组姓名/2组姓名"配对展示（如"周海清/张一鸣"）

**映射规则**：原始数据的第 N 段（第 N 个评测人）对应测评人信息表中序号 N。

---

### 打分选项

| T2V/T2AV | WM | 含义 |
|----------|-----|------|
| G (Good) | G（Good） | 模型A 明显更好 |
| As good | AS good | 两者差不多，A 稍好 |
| As bad | AS bad | 两者差不多，B 稍好 |
| B (Bad) | B（Bad） | 模型B 明显更好 |

> 系统需兼容两种括号格式，建议按场景配置或自动识别。

---

## 五、输出产物

### 输出 1：汇总结果（所有场景）

按"N人总值" + 各评测人分别输出统计块。

**每个统计块格式**：

| 能力类型 | 模型A数 | As good数 | As bad数 | 模型B数 | 模型A占比 | As good占比 | As bad占比 | 模型B占比 | Similar占比 | GSB |
|---------|---------|----------|---------|---------|-----------|------------|-----------|----------|------------|-----|
| [一级维度1] | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |
| [一级维度2] | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |
| ... | | | | | | | | | | |

**表头中的模型名称**：从原始数据的"模型A"/"模型B"列名或 URL 中提取（WM 示例中为 "JoyAI-WM" 和 "Happy-oyster"）。

**计算逻辑**：

1. **个人统计**：对该评测人的所有行，按每个一级维度列 COUNTIF 统计 G / As good / As bad / B 数量
2. **N人总值**：所有评测人的对应计数相加
3. **占比** = 数量 / prompt总数（个人），或 数量 / (prompt总数 × 人数)（总值）
4. **Similar占比** = As good占比 + As bad占比
5. **GSB** = (G占比 + 0.5 × Similar占比) / (B占比 + 0.5 × Similar占比)

---

### 输出 2：维度汇总（所有场景）

按标签体系的每个标签值分组统计各一级维度的 GSB。

**结构**：横向每 3 个标签值并列为一组。

```
[标签体系名称]（如 task_type / 主体类型）
  ┌─ 标签值1 ─────────────┬─ 标签值2 ─────────────┬─ 标签值3 ─────────────┐
  │ N人平均值              │ N人平均值             │ N人平均值              │
  │ [维度1]: B/Ag/Ab/G占比  │ [维度1]: ...          │ [维度1]: ...          │
  │ [维度2]: ...           │ [维度2]: ...          │ [维度2]: ...          │
  │ ...                    │ ...                  │ ...                   │
  ├────────────────────────┼──────────────────────┼───────────────────────┤
  │ 计数明细               │ ...                  │ ...                   │
  │ (B/Ag/Ab/G/Total)     │                      │                       │
  └────────────────────────┴──────────────────────┴───────────────────────┘
```

**计算逻辑**：

1. 筛选原始数据中"标签列 = 某标签值"的所有行
2. 对筛选结果，统计各一级维度的 G / As good / As bad / B 数量
3. 多选标签（T2V）：主列和副列都参与匹配
4. "N人平均值"行 = 各计数求和后计算占比和 GSB
5. 横向布局：每 3 个标签值并列为一组

---

### 输出 3：二级维度汇总（所有有影响因素的场景）

| 一级分类 | 二级分类 | Good方向次数 | Bad方向次数 | 合计 |
|---------|---------|------------|-----------|------|

**计算逻辑**：

1. 遍历原始数据汇总的全部行
2. 对每个一级维度的打分列，解析对应的"影响因素"JSON 数组
3. **Good 方向**（G (Good) 或 As bad 打分）：影响因素表示"模型A好在哪里"
4. **Bad 方向**（B (Bad) 或 As good 打分）：影响因素表示"模型A差在哪里"
5. 分别统计每个二级维度标签在两个方向被提及的次数
6. "综合评价"的影响因素中会出现一级维度名称，作为综合评价的二级分类也参与统计
7. 过滤掉空字符串因素

> 只要原始数据中维度列后有影响因素列（空表头列），即产出此表。不区分场景。

---

### 输出 4：个人数据汇总

在原始数据基础上，按 prompt 聚合所有评测人打分，生成每条 prompt 的综合结论。

**结构**：

| 序号 | prompt相关列 | 模型A | 模型B | [综合列: 每个一级维度] | [评测人1: 各维度打分] | [评测人2: 各维度打分] | ... |

**综合列计算**（每条 prompt × 每个一级维度）：
- 统计 N 人中选 G / As good / As bad / B 的人数
- GSB = (G + 0.5 × (As_good + As_bad)) / (B + 0.5 × (As_good + As_bad))
- GSB > 1 → 显示 "G（Good）"，GSB < 1 → "B（Bad）"，GSB = 1 → 取第一个评测人的值（IFERROR 兜底）

---

## 六、数据处理流程

```
┌──────────────────────────────────┐
│     配置（预设，版本迭代时修改）    │
│                                  │
│  ① 评测维度体系（一级+二级维度）   │
│     来源: 视频生成通用评测维度汇总  │
│                                  │
│  ② 标签体系                      │
│     T2V: 6套标签体系              │
│     WM: 1套（task_type）          │
│                                  │
│  ③ 评测场景选择（T2V/T2AV/WM）    │
└──────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────┐
│     输入（用户每次上传）           │
│                                  │
│  ① 原始数据汇总表（核心）         │
│     = 标签 + 打分 [+ 影响因素]    │
│     = N人 × M条 prompt           │
│                                  │
│  ② 测评人信息表（辅助）           │
│     = 姓名列表 / 分组配对         │
└──────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────────┐
│              自动计算引擎                      │
│                                              │
│  Step 1: 解析原始数据                         │
│    → 自动识别评测人数（序号列重复周期）         │
│    → 自动识别 prompt 总数                     │
│    → 匹配一级维度列、标签列                   │
│                                              │
│  Step 2: 产出"汇总结果"                       │
│    → 按评测人分组 COUNTIF                     │
│    → 求和得总值 → 占比 → GSB                  │
│                                              │
│  Step 3: 产出"维度汇总"                       │
│    → 按标签体系 × 标签值 COUNTIFS             │
│    → 占比 → GSB                              │
│                                              │
│  Step 4: 产出"二级维度汇总"（仅T2V/T2AV）     │
│    → 遍历影响因素列，解析 JSON                 │
│    → 按二级维度 key 计数                      │
│                                              │
│  Step 5: 产出"个人数据汇总"                    │
│    → 按 prompt 聚合，计算 N 人综合结论         │
└──────────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────┐
│        输出 Excel                 │
│  ├── 汇总结果                    │
│  ├── 维度汇总                    │
│  ├── 二级维度汇总（T2V/T2AV）     │
│  └── 个人数据汇总                 │
└──────────────────────────────────┘
```

---

## 七、关键公式

### GSB 计算

```
GSB = (G_count + 0.5 × Similar_count) / (B_count + 0.5 × Similar_count)
```

其中 `Similar_count = As_good_count + As_bad_count`

- GSB > 1：模型 A 整体更优
- GSB < 1：模型 B 更优
- GSB = 1：两者持平

### 占比计算

- 个人占比 = 维度计数 / prompt 总数
- 总值占比 = 总维度计数 / (prompt 总数 × 评测人数)

---

## 八、动态适配规则

| 项目 | 识别方式 |
|------|---------|
| 评测人数量 | 序号列重复周期自动推断 |
| Prompt 总数 | 单轮序号最大值 |
| 一级维度 | 按配置的维度名称匹配表头列名 |
| 标签列 | 按配置的标签体系名称匹配表头列名 |
| 模型名称 | 从"模型A"/"模型B"列名提取，或从个人数据汇总表头提取 |
| 打分格式 | 按场景配置（半角 vs 全角括号）或自动探测 |
| 是否有影响因素列 | 按场景配置决定是否解析 |

---

## 九、交互流程

1. 用户选择评测场景（T2V / T2AV / WM）
2. 系统加载对应的评测维度体系和标签体系配置
3. 用户上传"原始数据汇总"表 + "测评人信息"表
4. 系统自动推断评测人数、prompt 数量、列映射，关联评测人姓名
5. 系统执行自动计算
6. 输出完整 Excel 文件（各 sheet 按场景决定是否包含）

---

## 十、配置示例（WM 场景实例）

基于 `评测数据示例1.xlsx`：

- **场景**：WM
- **评测人数**：12
- **Prompt 总数**：200
- **一级维度**：语义跟随、初始世界一致性、运动性、空间时序一致性、视觉美学、综合评价
- **标签体系**：task_type（autonomous_driving / embodied_robot / indoor_home / multi_social / natural_outdoor / virtual_world）
- **模型名称**：JoyAI-WM vs Happy-oyster
- **打分格式**：G（Good）/ AS good / AS bad / B（Bad）
- **影响因素**：无
- **输出 sheet**：汇总结果、维度汇总、个人数据汇总（无二级维度汇总）
